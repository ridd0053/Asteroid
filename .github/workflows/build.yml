Name: Build

on:
  push:
    branches:
    - feature_branch_pipeline
stages:
  - build
build:
  stage: build
  script:
    - echo "Building"
     artifacts:
    paths:
      - build/
test:
  stage: test
  script:
    - echo "Testing"
    - test -f "build/info.txt"
